services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    environment:
      - PORT=3001

  frontend:
    build: ./frontend
    ports:
      - "5001:5001"
    depends_on:
      - backend
