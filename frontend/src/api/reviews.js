/**
 * API client for review endpoints (home feed, etc.).
 */

import { API_BASE } from "./config.js";

/**
 * Fetches review cards for the home page feed.
 * TODO: GET `${API_BASE}/reviews/feed` (or `/home/reviews`) and return data.reviews — import API_BASE from ./config.js
 * @returns {Promise<Array>}
 */
export async function fetchHomeReviews() {
  return [];
}
